services:
  - type: web
    name: love-doll-search
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python json_to_db.py
    startCommand: gunicorn app:app --timeout 120 --workers=1
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0